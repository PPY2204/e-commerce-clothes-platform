server:
  port: 8080

spring:
  application:
    name: review-service
  data:
    mongodb:
      uri: ${REVIEW_DB_URI:mongodb://mongodb:27017/review_db}

eureka:
  client:
    enabled: false
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
